<h1>Images:</h1>

  <% @images.each do |image|%>
    <p><%= image.title %></p>
    <p><%= link_to(image_tag(image.picture.url(:large)), user_image_path(@user, image) ) %></p>

  <% end %>

  <%= link_to "Add an Image", new_user_image_path(@user) %>
